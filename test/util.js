const mocha = require('mocha');
const assert = require('assert');
const util = require('../src/util');

describe('util', () => {
  describe('checks', () => {

  });

  describe('errors', () => {

  });
});