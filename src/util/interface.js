/**
 * Type of argument array of points mode.
 * @type {POINTS_MODE}
 */
const POINTS_MODE = Object.freeze({
  ITERABLE: 'iterable',
  OBJECT: 'object',
  ERROR: 'error'
});

module.exports = {
  POINTS_MODE
};
